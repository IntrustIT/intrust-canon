# Reference helpers + hooks

These files are **not** imported by Intrust apps at runtime. They're reference implementations of the helpers + hooks described in `../memory/*.md`.

Same model as `components/`: each consumer keeps its own copy, the docs are authoritative, this directory is a concrete example.
